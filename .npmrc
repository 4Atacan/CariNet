strict-peer-dependencies=false
auto-install-peers=true

# NOT: pnpm 11 fetch-timeout / network-concurrency gibi ayarlari .npmrc'den DEGIL
# pnpm-workspace.yaml'dan okur (nodeLinker ile ayni tuzak). Oraya bakin.
